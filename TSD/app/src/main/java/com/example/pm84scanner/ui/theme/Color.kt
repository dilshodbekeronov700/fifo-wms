package com.example.pm84scanner.ui.theme

import androidx.compose.ui.graphics.Color

val Color_White = Color(0xFFFFFFFF)

// ── Brend ranglari (zamonaviy, sklad uchun yorqin) ───────────────────────────
val BrandIndigo      = Color(0xFF2541B2)   // asosiy
val BrandIndigoDark  = Color(0xFF13205C)
val BrandIndigoLight = Color(0xFF5B73E8)
val BrandCyan        = Color(0xFF00C2CB)   // accent (gradient juftligi)

// Neytral
val Ink        = Color(0xFF0D1B2A)
val InkSoft    = Color(0xFF415A77)
val Surface    = Color(0xFFF4F6FB)
val SurfaceCard = Color(0xFFFFFFFF)

// Holat ranglari
val Ok      = Color(0xFF1FA971)
val Warn    = Color(0xFFF59E0B)
val Danger  = Color(0xFFE5484D)

// ── Har oqim uchun accent (Dashboard kartalari va ekran sarlavhalari) ─────────
val FlowProduction = Color(0xFF2541B2)  // Ishlab chiqarish
val FlowCheck      = Color(0xFF0EA5E9)  // Tekshiruv
val FlowImport     = Color(0xFF7C3AED)  // Import
val FlowPutaway    = Color(0xFF1565C0)  // Joylash
val FlowReceipt    = Color(0xFF0D9488)  // Kirim
val FlowPicking    = Color(0xFF6D28D9)  // Terish
val FlowInventory  = Color(0xFFEA580C)  // Inventarizatsiya
val FlowLabel      = Color(0xFF475569)  // Yorliq
val FlowTasks      = Color(0xFF16A34A)  // Vazifalar

// ── Dark mode ─────────────────────────────────────────────────────────────────
val DarkBg      = Color(0xFF0B1020)
val DarkCard    = Color(0xFF161C30)
val DarkInk     = Color(0xFFE7EaF3)
